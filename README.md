# Slides for introduction to learnability

The slides is based on [slidev](https://sli.dev).

## Usage

Install dependency:

```bash
yarn install
```

Start the slides:

```bash
yarn run dev
```

Export to pdf:

```bash
yarn run build
```